export interface ErrorMessageProps {
    message: string;
}

export interface InsightDisplayProps {
    insightText: string;
}

export interface InsightRequestProps {
    company_url: string;
    user_notes: string;
}